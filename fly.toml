# fly.toml app configuration file generated for yearn-metrics on 2026-03-13T14:57:10Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'yearn-metrics'
primary_region = 'iad'

[build]

[env]
  PORT = "3456"
  DB_PATH = "/data/yearn-tvl.db"

[mounts]
  source = "data"
  destination = "/data"

[http_service]
  internal_port = 3456
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

  [[http_service.checks]]
    interval = "30s"
    timeout = "5s"
    grace_period = "10s"
    method = "GET"
    path = "/health"

[[vm]]
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
